# Multi_class_Classification_workflow_Template

This project was split from the monorepo notebook collection and contains one notebook with isolated dependencies.

## Files
- `Multi_class_Classification_workflow_Template.ipynb`
- `requirements.txt`

## Setup
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
jupyter lab
```

## Portfolio Context
This notebook matches the classification-oriented project direction on
`tendaisibanda.dev`, where multiple model choices are compared using consistent
metrics and clear experiment tracking.
