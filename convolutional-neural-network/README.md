# CNN

This project was split from the monorepo notebook collection and contains one notebook with isolated dependencies.

## Files
- `convolutional_neural_network.ipynb`
- `requirements.txt`

## Setup
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
jupyter lab
```

## Portfolio Context
This notebook is consistent with the CNN-focused computer vision work highlighted on
`tendaisibanda.dev` (FashionMNIST classification and architecture comparison).
Use it as a focused CNN implementation track alongside the broader comparison notebooks.
