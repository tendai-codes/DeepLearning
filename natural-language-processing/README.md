# natural_language_processing

This project was split from the monorepo notebook collection and contains one notebook with isolated dependencies.

## Files
- `natural_language_processing.ipynb`
- `requirements.txt`

## Setup
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
jupyter lab
```

## Portfolio Context
This notebook aligns with the NLP work showcased on `tendaisibanda.dev`, including
text preprocessing and classical machine-learning pipelines for language tasks.
