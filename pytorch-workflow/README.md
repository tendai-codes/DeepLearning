# PyTorch_Workflow

This project was split from the monorepo notebook collection and contains one notebook with isolated dependencies.

## Files
- `PyTorch_Workflow.ipynb`
- `requirements.txt`

## Setup
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
jupyter lab
```

## Portfolio Context
This notebook serves as a lightweight template for the model development process
used across projects on `tendaisibanda.dev`: prototype, evaluate, and refine.
